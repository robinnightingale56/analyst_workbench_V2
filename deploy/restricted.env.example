# Non-secret baseline only. Store actual configuration outside the release.
AUTH_MODE=pki
DEPLOYMENT_PROFILE=restricted
NODE_ENV=production
PORT=8080
# DATABASE_URL must be supplied separately by the target administrator.
# PKI intentionally fails closed until an approved identity adapter exists.
# No credentials, trust material, external provider keys, or identity headers.